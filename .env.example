# Better Auth Server URL
# This should point to your separate Better Auth server instance.
# Must NOT be the Next.js app's own origin (http://localhost:3000),
# or every auth call will hit this app instead of the auth server.
NEXT_PUBLIC_AUTH_SERVER_URL=http://localhost:3001
